#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*_strlen- returns the lenght of a string
*
*@s: holds the  string
*Return: always 0 (success)
*/
int _strlen(char *s);
{
	strlen(s);
	return (0);
}
