#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*_strlen- returns the lenght of a string
*
*@s: holds the  string
*Return: lenght of s
*/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
